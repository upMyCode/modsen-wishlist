import { Dimensions } from 'react-native';

export const WindowDimensions = {
  windowWidth: Dimensions.get('window').width,
  windowHeight: Dimensions.get('window').height,
};

export const SignUpFormDimensions = {
  buttonWidth: 350,
  buttonHeight: 48,
  buttonBRadius: 14,
  buttonMTop: 20,
  inputMLength: 30,
  inputMLengthXL: 16,
};
